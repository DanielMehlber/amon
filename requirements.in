# Full developer dependency set. Compile locally (do not commit the lockfile):
#   pip-compile --strip-extras -o requirements.txt requirements.in
#   pip-sync requirements.txt
# Portable / offline installs use requirements-runtime.in instead.
opencv-python-headless
numpy
pyyaml
pillow
panel
pandas

# Test dependencies
pytest
